# 个人网站重设计

董西垣个人网站的静态重设计版本，以“把直觉变成可以被验证的模型”为核心叙事。

## 本地预览

在当前目录执行：

```bash
python3 -m http.server 4173
```

然后访问 `http://localhost:4173`。

## 文件

- `index.html`：页面内容与语义结构
- `styles.css`：视觉系统与响应式样式
- `script.js`：导航、滚动轨迹、渐入与复制交互
- `photo_副本.jpg`：保留的原始照片素材，当前页面未引用
